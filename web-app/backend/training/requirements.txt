# Training-only dependencies.
# These are SEPARATE from web-app/backend/requirements.txt because the
# runtime backend only needs `torch` + `torchvision` for inference, not
# the training stack (timm, kagglehub, sklearn, etc.).
#
# Install with:
#   cd web-app/backend
#   python -m venv .venv-train
#   .venv-train\Scripts\activate          (Windows)
#   source .venv-train/bin/activate       (Linux)
#   pip install -r training/requirements.txt
#
# IMPORTANT — for GPU, install torch FIRST with the CUDA wheel matching
# your driver. Example for CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Then install the rest with the line below.

torch>=2.2
torchvision>=0.17
timm>=1.0           # ResNet50 backbone + pretrained ImageNet weights
numpy>=1.26
pandas>=2.0
pillow>=10.0
scikit-learn>=1.3   # stratified train/val/test split
tqdm>=4.66
matplotlib>=3.8     # training curves (optional but nice for the report)
kagglehub>=0.3      # one-line UTKFace + FER2013 downloads
