fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic>=2.11
pydantic-settings>=2.6
python-dotenv==1.0.1
httpx==0.27.2
python-multipart==0.0.12
google-genai
openai-whisper
edge-tts
soundfile
pyyaml

# ── Vision (age + mood) — runtime inference ─────────────────────────
# Loads the team's trained PyTorch weights produced by
# web-app/backend/training/. NO TensorFlow / DeepFace — both models are
# pure PyTorch (ResNet50 + EfficientNet-B0).
# For GPU install torch with the matching CUDA wheel BEFORE pip-installing
# this file:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
torchvision>=0.17
timm>=1.0
opencv-python-headless>=4.10
pillow>=10.0
