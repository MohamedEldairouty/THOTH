fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic>=2.11
pydantic-settings>=2.6
python-dotenv==1.0.1
httpx==0.27.2
python-multipart==0.0.12
google-genai
openai-whisper
edge-tts
soundfile
pyyaml

# ── Vision (age + mood) ──────────────────────────────────────────────
# DeepFace ships with TensorFlow and gives both age + 7-class emotion
# from one analyse() call. Heavy install (~600MB with tf-cpu) but
# eliminates the need to host & maintain separate model files until the
# vision team ships their custom SSR-Net + EfficientNet weights.
# Swap point: env THOTH_VISION_BACKEND=team will load their .keras/.pth
# from web-app/backend/models/ instead (same VisionService interface).
deepface>=0.0.93
tensorflow-cpu>=2.16; sys_platform != "darwin"
tensorflow>=2.16; sys_platform == "darwin"
opencv-python-headless>=4.10
pillow>=10.0
